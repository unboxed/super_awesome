extends ../layout

block body
  p!= description.replace(/\n/g,"<br />")
    form(method="POST" action="/polls/"+uuid+"/votes")
      input(type="text" id="choice" name="choice" placeholder="Your say here" class="form-control")
      br
      button(type="submit" class="btn btn-success") Vote
      
